const certif = document.querySelector(".python-certificate");
window.onload = () => {
  certif.addEventListener("click", (evnt) => {

    if (evnt.target) {

    }
  });
};

const createdModal = () => {
  
}